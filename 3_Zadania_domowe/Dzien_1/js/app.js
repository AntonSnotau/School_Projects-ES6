//1//
const tab = [4, 5, 7];
function getNumbers(arr) {
  arr.map(item => item * 2);
};
getNumbers(tab);

//2//
function Weather(arr) {
  this.temp = arr;
  function getAvgTemperature() {
    
  }
}

const newweath = new Weather('[10, 15, 13]');
